+++
title = "A page with a remote cover"
cover = "https://cdn.example.test/cover.jpg"
coverCaption = "A cover held on another host, left as it was written."
noindex = true
+++

A generic page whose cover lives on another host. It is here so the difference
between a remote cover and a local one is exercised rather than described: a
remote URL must be passed through untouched, where a local path is joined to the
site's base URL.
