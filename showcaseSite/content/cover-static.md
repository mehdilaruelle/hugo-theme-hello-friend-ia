+++
title = "A page with a cover from static"
cover = "/img/cover.png"
coverCaption = "A cover under `static/`, joined to the site's base URL."
noindex = true
+++

A generic page whose cover is a path under `static/`. Hugo cannot measure it, so
no dimensions are emitted — but the URL has to carry the site's subpath.

A page may of course link to [the page kept out](https://example.com/subpath/showcase/kept-out/) in prose.
